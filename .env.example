# API Configuration
# For development with Vite proxy (recommended): No configuration needed!
# The app uses /api/v1 by default, which Vite proxies to http://localhost:8080/api
# Only create a .env file if you need to override the default behavior:

# For production or direct API access (without proxy): Uncomment and set full URL
# VITE_API_BASE_URL=http://localhost:8080/api/v1
# VITE_API_BASE_URL=https://your-api-domain.com/api/v1
